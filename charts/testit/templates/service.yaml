apiVersion: v1
kind: Service
metadata:
  name: {{ include "testit.fullname" . }}
  labels:
    {{- include "testit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testit.selectorLabels" . | nindent 4 }}
